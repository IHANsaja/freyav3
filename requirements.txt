# ── Core (Gemini Live voice assistant) ──
# NOTE: browser-use pins google-genai<2, so this stack runs on google-genai 1.65.x.
google-genai
python-dotenv
pyaudio
keyboard
pyautogui
pillow
fastapi
uvicorn
websockets
mss
pytesseract

# ── Superpowers ──
mcp                 # Model Context Protocol client (any MCP server)
browser-use         # autonomous Chromium agent (needs: playwright install chromium)
playwright
uiautomation        # pixel-perfect Windows UI Automation screen control
pywin32
pygetwindow         # window management
pyperclip           # clipboard
send2trash          # Recycle Bin deletes (core/file_manager.py)
pycaw               # system volume
comtypes
psutil
numpy
chromadb            # local vector store for semantic RAG memory
pygame              # GPU shader overlay window (Crimson Core Sync screen-control FX)
moderngl            # GLSL fragment-shader rendering for the overlay
