# ── Core (Gemini Live voice assistant) ──
google-genai>=2.23.0,<3
httpx>=0.27,<1
pydantic>=2,<3
python-dotenv
pyaudio
keyboard
pyautogui
pillow
fastapi
uvicorn
websockets
mss
pytesseract

# ── Superpowers ──
mcp                 # Model Context Protocol client (any MCP server)
playwright          # drives Chromium for Freya's own browser (core/browser).
                    # needs: playwright install chromium
beautifulsoup4      # readable-text extraction for web_fetch
uiautomation        # pixel-perfect Windows UI Automation screen control
pywin32
pygetwindow         # window management
pyperclip           # clipboard
send2trash          # Recycle Bin deletes (core/file_manager.py)
pycaw               # system volume
comtypes
psutil
numpy
chromadb            # local vector store for semantic RAG memory
moderngl            # GLSL rendering for the desktop overlay (core/shaders/*.glsl)

pypdf>=6,<7       # local PDF text extraction, no cloud vision
pypdfium2>=4,<6    # render a selected scanned PDF page for local OCR
